{
  "name": "core-auth",
  "version": "1.0.0",
  "description": "better-auth service for Core OSS — email/password authentication",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "migrate": "bunx @better-auth/cli@latest migrate",
    "generate": "bunx @better-auth/cli@latest generate"
  },
  "dependencies": {
    "better-auth": "^1.2.7",
    "hono": "^4.7.5",
    "pg": "^8.13.3"
  },
  "devDependencies": {
    "@types/pg": "^8.11.11",
    "@better-auth/cli": "latest",
    "bun-types": "latest"
  }
}
